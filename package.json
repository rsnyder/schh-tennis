{
  "name": "schh-tennis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:pages": "wrangler deploy --dry-run --outdir .wrangler/pages-build && mkdir -p pages/dist && cp .wrangler/pages-build/index.js pages/dist/_worker.js",
    "deploy:pages": "npm run build:pages && cd pages && wrangler pages deploy --commit-dirty=true",
    "deploy:all": "npm run deploy && npm run deploy:pages",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "@types/node": "^26.2.0",
    "typescript": "^5",
    "vitest": "^3",
    "wrangler": "^4"
  }
}
